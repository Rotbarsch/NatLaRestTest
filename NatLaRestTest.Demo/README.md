# NatLaRestTest.Demo

A collection of demo features demonstrating the capabilities of the NatLaRestTest library.

## Changing the port
If you need to change the port that the DemoWebApi service listens on, update the base URL in `NatLaRestTest.Demo/NatLaRestTestSettings.Development.json`, specifically the `demoApiBaseUrl` variable under `globalVariables`.